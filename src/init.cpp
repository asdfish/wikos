#include <files.hpp>
#include <init.hpp>

#include <string>
#include <vector>
#include <filesystem>

int init(void) {
  std::vector<std::string> directories;
  directories.append(directory_directories("."));

  return 0;
}
